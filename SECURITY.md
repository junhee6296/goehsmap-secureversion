# 보안 운영 정책

## 공개 범위

사이트가 공개하는 학교 필드는 좌표, 학교급, 학교명, 주소, 설립 구분, 학생·교원·학급 수, 표시 색상, 홈페이지, 특색사업뿐입니다. 교육지원청 레코드와 교장·교감·행정실장 필드는 생성 단계와 응답 단계에서 각각 제거합니다.

로그인, 회원가입, 세션, 쿠키, 사용자 DB, 개인 메모, 즐겨찾기, 관리자 화면, 쓰기·수정·삭제 API는 존재하지 않습니다.

## 적용 통제

- 운영 서버는 정제된 로컬 JSON만 읽으며 원본 Google Sheet 또는 인증키에 접근하지 않습니다.
- 공개 파일 경로를 허용목록으로 제한해 `.env`, 소스 서버 파일, 패키지 파일, 정제 전 데이터 파일을 웹에서 내려받을 수 없습니다.
- CSP, HSTS, 클릭재킹 방지, MIME 스니핑 방지, Referrer-Policy, Permissions-Policy를 적용합니다.
- API 요청 횟수 제한, 외부 입력 길이 제한, URL·색상·좌표 형식 검증을 적용합니다.
- 서버 오류 응답에는 스택·파일 경로·환경변수 값을 포함하지 않습니다.
- 런타임은 `127.0.0.1:3001`에만 바인딩하고 외부 연결은 Nginx의 HTTPS 가상호스트로만 받습니다.

## 운영 점검표

- 배포 전 `npm ci`, `npm run check`, `npm test`, `npm audit --omit=dev`를 통과시킵니다.
- `.env.sync`와 원본 시트 ID를 운영 서버에 배포하지 않습니다.
- 원본 시트 자체의 공개 공유를 해제하거나, 인사정보가 없는 별도 관리용 원본으로 교체합니다.
- 방화벽에서 외부 공개 포트는 80/443만 허용하고 3001은 열지 않습니다.
- OS와 Node.js LTS 보안 업데이트를 정기 적용합니다.
- TLS 인증서 자동 갱신과 만료 알림을 확인합니다.
- 로그에 요청 본문, 환경변수, 원본 데이터가 남지 않는지 점검합니다.
- 정제 데이터 갱신 때마다 생성 파일의 변경 내역과 테스트 결과를 승인 기록으로 남깁니다.

## 신고

취약점 신고는 경기도화성오산교육지원청 공식 홈페이지의 대표 연락처를 이용해 주세요. 민감한 정보는 공개 이슈에 작성하지 마세요.

이 문서는 일반적인 행정 보안성 검토에 필요한 통제를 구현한 내역이며, 특정 기관의 공식 적합 판정이나 인증을 대신하지 않습니다.
